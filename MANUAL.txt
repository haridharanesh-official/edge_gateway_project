
EDGE GATEWAY DOCKER MANUAL

1. Install Docker & Docker Compose
2. Extract this zip
3. Run:
   docker-compose up --build

4. To test MQTT:
   mosquitto_sub -h localhost -t iot/device1/data

5. Simulate offline:
   disconnect internet

EXPECTED:
- Offline → data stored in SQLite
- Online → data published + cache cleared
